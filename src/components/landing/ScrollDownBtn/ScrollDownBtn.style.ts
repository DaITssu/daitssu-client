import styled from '@emotion/styled';

export const StyledScrollDownBtn = styled.button`
  background: transparent;
  border: none;

  position: absolute;
  bottom: 20px;
  left: 50%;
  transform: translate(-50%, 0%);
`;
