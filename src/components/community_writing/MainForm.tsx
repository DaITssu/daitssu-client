

interface MainFormnProps {
  label?: string;
  width?: number;
  height?: number;
  color?: string;
  onClick?: () => void;
}

const MainForm = ({ }: MainFormnProps) => (
  <div>

  </div>
);

export default MainForm;
